extends layout 

block content
  form(action='' method='POST')
    fieldset 
      legend Utwórz konto
      if errors 
        ul(style={color: 'red'}) 
          for error in errors 
            li!=error.msg
      if message
        p(style={color: 'red'}) #{message}
      label(for='username') Wybierz nazwę użytkownika
        span(aria-label='required') *
      input(type='text' name='username'  required value=username)
      label(for='password') Hasło
        span(aria-label='required') * 
        | (musi mieć conajmniej 6 znaków)
      input(type='password' name='password' minlength='6' maxlength='18' required)
      button(type='submit') Submit
  br
  a(href='/') Zaloguj się